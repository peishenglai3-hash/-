# 更新日志

## [未发布] - 2026-08-13

### 第一章角色素材

- 为 `CH01_SC01` 的正面、背面、左侧、右侧四组行走素材完成绿幕抠图；每个方向保留 8 张按文件名排序的源帧，生成透明 PNG 帧与运行时等比 `720px` 高版本。
- 四组帧均使用软色度键、最大连通域清理、半透明边缘去绿和每方向八帧并集裁切；不逐帧平移或单独裁切，以保留原始步态与脚底锚点。
- 场景一主角行走由旧横向 spritesheet 改为逐帧加载四方向透明 PNG；正面八帧在导出时统一水平镜像，四方向均以 `12fps` 循环播放，静止时回到当前朝向的首帧。
- `scripts/process-modern-player-assets.py --target ch01-sc01` 可重建场景一角色素材、透明预览、处理配方和 QA 报告。

### 验证

- `python scripts/process-modern-player-assets.py --target ch01-sc01`：四方向 QA 通过。
- `npx tsc --noEmit`
- `pnpm run build`
- 浏览器运行时验证：32 张帧纹理与四条行走动画均成功注册并可播放。

## [未发布] - 2026-08-12

### 开发者工具

- 将 `35a4152` 中的开发者编辑器迁移到 Vue/TypeScript 主线，保留碰撞区、交互区、前景磁性套索、JSON 保存和章节跳转工具。
- `P` 键改为全局开发者模式开关；面板控件获得焦点时再次按 `P` 也能正常关闭。
- 开发者模式启用时仅禁用 Vue 游戏 UI 覆盖层的鼠标事件，保持 Phaser canvas 可点击，修复碰撞区域无法直接点选、拖动和缩放的问题。
- 编辑器输入改为直接监听游戏 canvas，并根据 canvas 的 CSS 缩放换算指针坐标，避免场景输入链或显示缩放造成命中偏移。
- 恢复人物脚底碰撞箱的显示与编辑；玩家碰撞箱可点选、拖动、缩放，并实时同步 Phaser 物理体和移动碰撞判定。
- 恢复“随机属性并跳到下一章”：`Scene01` 跳到 `PrologueScene02` 起点，`PrologueScene02` 跳到 `Ch01Sc01Scene` 起点，同时生成序章画像属性和必要剧情标记。
- 开发者工具扩展到全部现有可玩场景：`Scene01`、`PrologueScene02`、`Ch01Sc01Scene`。
- 编辑器支持按场景配置坐标比例：旧序章场景使用 `32px` 网格，第一章场景使用像素坐标；碰撞区、交互区、人物碰撞箱和前景套索共享同一套编辑逻辑。
- 开发服务器保存白名单新增 `public/data/ch01_sc01_chen_home_wake_manifest.json`，第一章编辑结果可直接写回场景 JSON。

### 验证

- `npm run build`
- 浏览器验证开发者面板开关、碰撞区点选与拖动、跨章节跳转，以及第一章开发者工具加载。

本文件记录项目的重要功能更新。

## [未发布] - 2026-08-11

### 新增

- 新增碰撞箱与交互区可视化编辑器，支持在游戏画面中直接查看和调整区域。
- 碰撞箱使用粉色显示，交互区使用黄色显示，当前选中区域使用白色边框和控制点显示。
- 支持按 `P` 键打开或关闭隐藏开发面板，正式界面不显示开发按钮。
- 支持点击选择、拖动移动，以及从四条边和四个角缩放区域。
- 鼠标靠近边缘或角落时显示对应的水平、垂直或对角缩放光标。
- 支持通过 `X`、`Y`、`W`、`H` 输入框精确调整位置和尺寸。
- 碰撞箱新增角度输入和可拖动旋转手柄，运行时使用分离轴定理处理玩家与斜向矩形的真实碰撞。
- 碰撞箱、交互区和前景套索统一支持复制及按地图竖直中轴水平镜像。
- 支持 `1`、`0.5`、`0.25`、`0.125` 逻辑格吸附；每个逻辑格为 `32px`。
- 支持新增和删除碰撞箱、交互区；新增区域默认位于地图中心并自动选中。
- 支持修改区域名称或稳定 ID，并将结果写回场景 JSON。
- 开发服务器提供受限保存接口；无法写入文件时自动下载 JSON 作为替代。
- 玩家和 NPC 的脚底碰撞框会实时跟随角色位置刷新，并可在碰撞箱页签中点选、拖动、缩放和写回 JSON。
- 编辑器打开期间保留玩家移动控制，可边移动边检查碰撞效果。
- P 键作为隐藏开发入口，不再默认显示可见的开发按钮。
- 开发面板支持随机生成序章选择与画像属性，并根据当前背景进入下一段剧情：场景一进入驻地剧情，场景二进入陈继南家穿越转场。
- 新增“前景套索”编辑模式，可在底图上自由框选任意多边形区域，并将该区域作为独立前景遮挡层。
- 前景套索改为磁性逐点路径：左键连续放置锚点并自动吸附底图边缘，右键单步撤销，双击沿边缘自动闭合完成，松开鼠标不会结束；闭合时会简化路径并自动修复可解的自交。
- 磁性套索会从当前场景底图生成 RGBA 梯度边缘图，并在点击点周围约 `14px` 范围内综合边缘强度与距离选择磁吸锚点。
- 相邻锚点之间使用自适应搜索走廊计算低代价边缘路径，优先贴合高对比轮廓，同时限制横向偏移与路径抖动；鼠标移动时以黄色路径实时预览下一段。
- 边缘图不可读取或分析失败时自动降级为直线锚点，不阻断套索编辑流程。
- 套索闭合后使用 Ramer-Douglas-Peucker 算法简化路径，按 `1/32` 逻辑格精度吸附并限制为最多 `512` 个顶点；随后通过区段反转修复可解的多边形自交。
- 前景套索支持选中、整体拖动、重命名、删除、复制和水平镜像；遮挡深度自动取多边形最低点。
- 运行时通过共享底图纹理与 Phaser `GeometryMask` 重建遮挡，不切割、不复制图片文件；关闭开发面板后遮挡仍然生效。
- 前景数据分别保存到 `scene01_manifest.json` 的 `foreground_occlusion.objects` 和 `PRO02_logic.json` 的 `foreground_layers.objects`。
- 内容校验新增前景多边形的唯一 ID、点数、有限数值、地图边界、面积与自交检查。
- 碰撞数据与前景蒙版更新已分流，拖动普通矩形不会重复重建背景遮罩；矩形和套索拖动均限制在地图边界内。

### 修复

- 前景遮挡改为比较前景多边形最低点与人物脚底碰撞箱底边，不再使用角色中心点或手工 `sort_y` 覆盖有效多边形。
- 场景一的主角、同学甲、同学乙脚底碰撞箱，以及场景二的主角脚底碰撞箱，均已接入 `P` 键编辑器的可点选修改流程。
- 场景一的 NPC 初始交互区与离场交互区改为数据驱动区域，开发模式会以黄色轮廓完整标注。
- 修复“随机属性并跳到下一章”从任意场景直接结算的问题；按钮现在按当前背景顺序进入对应剧情。

### 已知限制

- 场景一“同学乙”的拍照动画当前使用 DOM GIF，Canvas 前景套索暂时无法覆盖该 NPC；其余 Phaser 角色可正常遮挡。

### 开发入口

```text
P                              打开或关闭编辑器
前景套索 → 开始套索             左键加锚点，右键撤销，双击完成，Esc 取消
/?scene=02                     直接进入第二场景后按 P 打开
```

### 数据文件

- `public/data/scene01_manifest.json`
- `public/data/PRO02_logic.json`
- `public/data/PRO02_interactions.json`

### 验证

- `npm run build`
- `npm run test:content`
